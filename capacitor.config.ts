import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.lovable.raktaseva",
  appName: "Rakta-Seva Connect",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
